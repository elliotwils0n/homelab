apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: loki-ingress
  namespace: monitoring
spec:
  entryPoints:
    - web
    - websecure
  routes:
    - match: Host(`loki.home.arpa`)
      kind: Rule
      services:
        - name: loki-headless
          port: 3100
