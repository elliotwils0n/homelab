apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: tempo
  namespace: monitoring-project
spec:
  destination:
    name: in-cluster
    namespace: monitoring
  project: monitoring
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
  sources:
    - repoURL: 'https://grafana.github.io/helm-charts'
      targetRevision: 1.24.0
      chart: tempo
      helm:
        valueFiles:
          - $values/monitoring/services/tempo/values.yaml
    - repoURL: 'https://forgejo.home.arpa/homelab/homelab.git'
    #- repoURL: 'https://gitlab.home.arpa/homelab/homelab.git'
      targetRevision: master
      ref: values
    - repoURL: 'https://forgejo.home.arpa/homelab/homelab.git'
    #- repoURL: 'https://gitlab.home.arpa/homelab/homelab.git'
      targetRevision: master
      path: monitoring/services/tempo/
